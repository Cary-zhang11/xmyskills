# 推荐触发词

- 请把这份需求拆成测试用例，按XMind层级输出
- 按我给的模板生成 p0/p1 测试点
- 先给核心用例，不要完整报告
- 按 tc-p0-001 / tc-p1-001 编号输出
- 先做主流程与关键分支，兼容和UI后补

# 不建议触发本技能的情况

- 需要补齐 UI/浮层/机型兼容（改用 `testcases-expand-coverage`）
- 需要覆盖率审计与质量报告（改用 `testcases-audit-report`）
